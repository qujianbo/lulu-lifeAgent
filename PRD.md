# 生活管家 Agent PRD

## 1. 文档信息

- 产品名称：生活管家 Agent
- 使用入口：微信公众号
- 文档版本：v0.1
- 创建日期：2026-06-04
- 技术方向：Python、LangChain、LangGraph、当前最新 DeepSeek 模型、阿里云轻量应用服务器、Docker Compose
- 当前阶段：需求讨论稿

## 2. 产品背景

用户日常生活中存在大量低频但反复出现的管理任务，例如事项提醒、消费记录、天气与通勤关注、饮食安排、家庭事务、资讯获取、常见问题咨询等。传统工具通常功能分散，用户需要在日历、备忘录、新闻 App、搜索引擎、公众号和聊天工具之间切换。

本产品希望通过微信公众号提供一个低门槛的个人生活 Agent。用户像和助手聊天一样提交需求，系统通过大模型理解意图，并结合定时任务、记忆、工具调用和消息推送能力，完成日常生活管理、定时资讯供给和常见问题问答。

## 3. 产品目标

### 3.1 核心目标

- 让用户通过微信公众号完成日常生活事项管理。
- 支持定时、主动、个性化的信息推送。
- 支持常见生活问题的自然语言问答。
- 基于 LangGraph 构建可扩展的 Agent 工作流。
- 使用当前最新 DeepSeek 模型作为主要大模型能力。

### 3.2 MVP 目标

- 用户可以通过公众号发送文字消息创建、查询、修改、删除提醒事项。
- 用户可以订阅每日资讯简报，例如天气、热点新闻、财经摘要、科技资讯。
- 用户可以向公众号提问常见生活问题，并获得结构化回答。
- 系统可以在固定时间向用户主动推送消息。
- 管理端可以配置资讯源、推送时间、基础提示词和系统开关。

### 3.3 非目标

- MVP 阶段不做完整移动 App。
- MVP 阶段不做复杂多人家庭协作空间。
- MVP 阶段不做金融、医疗、法律等高风险领域的确定性建议。
- MVP 阶段不做完全自动消费、下单、转账等敏感操作。

## 4. 用户画像

### 4.1 主要用户

城市生活用户，已经频繁使用微信，希望用聊天方式管理生活中的琐事。

典型特征：

- 每天使用微信。
- 不愿意安装新 App。
- 有提醒、资讯、问答、记录等轻量需求。
- 接受 AI 辅助，但希望操作简单、结果可靠。

### 4.2 次要用户

个人效率工具爱好者、家庭事务管理者、自由职业者、创业者。

典型需求：

- 想用一个入口集中管理生活与轻办公任务。
- 需要早报、晚报、提醒、日程总结。
- 希望 Agent 能逐步记住个人偏好。

## 5. 核心使用场景

### 5.1 日常生活管理

用户通过公众号发送：

- “明早 8 点提醒我带身份证。”
- “每周三晚上提醒我倒垃圾。”
- “帮我记录今天花了 38 元买咖啡。”
- “我这个月有哪些待办？”
- “把明天 9 点的提醒改到 10 点。”
- “取消今天晚上的健身提醒。”

系统应能理解用户意图，抽取时间、事项、周期、优先级等信息，并通过微信消息完成确认与提醒。

### 5.2 定时资讯供给

用户通过公众号发送：

- “每天早上 8 点给我发天气和科技新闻。”
- “工作日早上推送财经早报。”
- “以后不要给我推娱乐新闻。”
- “今天有什么重要新闻？”

系统根据订阅配置，在指定时间生成并推送个性化简报。

### 5.3 常见问题问答

用户通过公众号发送：

- “空气炸锅怎么清洗？”
- “上海办理居住证需要哪些材料？”
- “感冒了能喝咖啡吗？”
- “怎么规划一周晚餐？”

系统提供清晰、可执行、带风险提示的回答。对于时效性、地域性、专业性较强的问题，应提示信息可能变化，并优先建议用户参考官方渠道。

### 5.4 个性化记忆

用户通过公众号发送：

- “以后默认我住在上海浦东。”
- “我不吃香菜。”
- “我上班通勤大概 45 分钟。”

系统将用户明确表达的偏好保存到用户画像中，用于后续回答和推送。

## 6. 功能需求

## 6.1 微信公众号入口

### 6.1.1 消息接收

系统应接收微信公众号用户发送的文本消息，并返回自然语言回复。

支持消息类型：

- 文本消息。
- 菜单点击事件。
- 关注与取消关注事件。
- 后续可扩展语音消息、图片消息、小程序跳转。

### 6.1.2 消息回复

系统应在微信公众号允许的响应窗口内尽快返回处理结果。

回复类型：

- 即时文本回复。
- 模板消息或订阅消息推送。
- 客服消息回复。

已确认方案：

- 使用已完成微信认证的服务号。
- 产品需要同时支持用户互动后的即时回复，以及定时提醒、每日简报等主动触达。
- 主动触达优先使用服务号可用的模板消息、订阅消息或客服消息能力，具体接口根据公众号后台权限落地。

### 6.1.3 菜单入口

公众号菜单建议包含：

- 我的提醒。
- 今日简报。
- 偏好设置。
- 更多能力。

点击菜单后，系统返回对应入口说明或直接触发查询。

### 6.1.4 标签与扩展入口

MVP 功能收窄为提醒、每日简报和生活问答，但需要在公众号侧预留扩展能力。

建议预留标签：

- `feature_reminder`：提醒与待办用户。
- `feature_briefing`：资讯简报用户。
- `feature_qa`：问答用户。
- `feature_record`：生活记录潜在用户。
- `feature_family`：家庭协作潜在用户。
- `feature_vip`：后续付费或高频用户。

标签用途：

- 区分不同用户能力开关。
- 支持后续灰度发布新功能。
- 支持差异化推送与运营分群。
- 为后续生活记录、家庭协作、知识库等扩展功能预留入口。

## 6.2 日常生活管理

### 6.2.1 提醒管理

用户可以创建一次性提醒和周期性提醒。

字段包括：

- 提醒标题。
- 提醒内容。
- 提醒时间。
- 重复规则。
- 时区。
- 状态。
- 用户确认状态。

系统行为：

- 当信息完整时直接创建提醒。
- 当时间或内容缺失时追问用户。
- 对模糊时间进行解释并确认，例如“明早”默认可解析为次日 08:00。
- 在提醒触发时主动推送微信消息。

### 6.2.2 待办管理

用户可以创建、查询、完成、删除待办。

示例：

- “帮我记一个待办：周末买洗衣液。”
- “今天还有什么没做？”
- “把买洗衣液标记为完成。”

MVP 阶段可将待办和提醒共用同一任务模型，通过是否有触发时间区分。

### 6.2.3 生活记录

支持轻量记录：

- 消费记录。
- 饮食记录。
- 运动记录。
- 情绪记录。
- 灵感记录。

MVP 建议先支持纯文本记录与标签分类，不做复杂统计。

### 6.2.4 每日总结

系统可在每天晚上推送生活总结：

- 今日完成事项。
- 未完成事项。
- 明日提醒。
- 今日记录摘要。

MVP 可作为可选订阅能力。

## 6.3 定时资讯供给

### 6.3.1 资讯订阅

用户可以订阅一个或多个资讯模块：

- 天气。
- 热点新闻。
- 科技。
- 财经。
- 体育。
- 本地生活。
- 日历提醒。

订阅字段：

- 订阅类型。
- 推送时间。
- 推送频率。
- 用户偏好。
- 是否启用。

### 6.3.2 简报生成

系统根据资讯源和用户偏好生成简报。

简报结构建议：

- 今日天气。
- 今日提醒。
- 重要新闻 3-5 条。
- 个性化关注内容。
- 一句话建议。

### 6.3.3 资讯源

MVP 可接入：

- 天气 API。
- 新闻 RSS。
- 可配置新闻 API。
- 手动配置白名单来源。

要求：

- 标注关键信息来源。
- 控制长度，适合微信阅读。
- 避免夸张标题和未经验证的内容。

已确认方案：

- MVP 阶段使用天气 API 加免费 RSS 或开放新闻接口。
- 新闻内容采用“标题 + 简短摘要 + 来源”的形式，不搬运长篇原文。
- 简报可由 AI 基于来源内容重新组织成适合微信阅读的短摘要。
- 第一版不强依赖付费资讯 API，但保留可配置新闻 API 接口。
- 暂不做全网实时搜索，优先使用白名单资讯源，降低质量和合规风险。

## 6.4 常见问题问答

### 6.4.1 通用问答

系统通过当前最新 DeepSeek 模型回答生活类问题。

回答要求：

- 简洁、实用、可执行。
- 对不确定问题主动说明限制。
- 对医疗、法律、金融等高风险问题给出风险提示。
- 对本地政策类问题建议参考官方渠道。

### 6.4.2 知识库问答

后续可接入自定义知识库：

- 家庭规则。
- 常用地址。
- 常用证件信息提示。
- 物业、维修、报销、办事流程。

MVP 阶段建议先预留 RAG 接口，不强制实现。

## 6.5 个性化与记忆

### 6.5.1 用户画像

用户画像包括：

- 城市。
- 作息偏好。
- 饮食偏好。
- 资讯偏好。
- 推送时间偏好。
- 禁忌或不喜欢的内容。

### 6.5.2 记忆写入策略

默认保存用户明确表达的长期偏好，用于后续问答、提醒和资讯推送的个性化。

示例：

- “以后都不要给我推体育新闻。”
- “我住在杭州。”
- “我不吃辣。”

临时对话内容不默认写入长期记忆。是否属于长期偏好，由 Agent 根据表达方式判断；存在歧义时可追问用户是否需要记住。

### 6.5.3 记忆管理

用户可以查询、修改、删除记忆。

示例：

- “你记住了我哪些偏好？”
- “删除我不吃辣这条。”
- “把我的城市改成深圳。”
- “删除我的所有记忆。”

系统应提供一键删除全部长期偏好的能力，并在删除前进行确认。

## 7. Agent 设计

## 7.1 总体架构

用户消息进入系统后，由 Agent 进行意图识别，再路由到不同工作流。

核心流程：

1. 微信公众号接收用户消息。
2. 后端校验微信签名并解析消息。
3. 消息进入 LangGraph Agent。
4. Agent 识别意图并选择工具。
5. 工具执行数据库读写、资讯拉取、定时任务创建等操作。
6. Agent 生成回复。
7. 后端将回复返回公众号或触发主动推送。

## 7.2 LangGraph 节点设计

建议节点：

- `input_guardrail`：输入校验、敏感内容识别、消息清洗。
- `context_loader`：加载用户画像、历史摘要、当前待办。
- `intent_router`：识别用户意图并路由。
- `task_agent`：处理提醒、待办、记录。
- `briefing_agent`：处理资讯订阅与简报生成。
- `qa_agent`：处理常见问题问答。
- `memory_agent`：处理用户偏好记忆。
- `tool_executor`：统一执行工具调用。
- `response_composer`：生成微信可读回复。
- `human_confirm`：对高风险或模糊操作进行确认。

## 7.3 意图分类

MVP 意图类型：

- `create_reminder`
- `update_reminder`
- `delete_reminder`
- `query_reminder`
- `create_todo`
- `complete_todo`
- `create_record`
- `subscribe_briefing`
- `unsubscribe_briefing`
- `query_briefing`
- `general_qa`
- `set_preference`
- `query_preference`
- `delete_preference`
- `unknown`

## 7.4 工具设计

Agent 可调用工具：

- `create_reminder_tool`
- `update_reminder_tool`
- `delete_reminder_tool`
- `query_reminders_tool`
- `create_record_tool`
- `query_records_tool`
- `get_weather_tool`
- `get_news_tool`
- `create_subscription_tool`
- `update_subscription_tool`
- `delete_subscription_tool`
- `get_user_profile_tool`
- `update_user_profile_tool`

工具返回结构化结果，再由 Agent 生成自然语言回复。

## 7.5 对话确认机制

以下场景必须确认：

- 删除多个提醒。
- 修改周期性提醒。
- 用户时间表达高度模糊。
- 涉及敏感个人信息。
- 可能导致频繁推送的订阅设置。

确认示例：

用户：“以后每天提醒我喝水。”

系统：“我理解为每天 09:00、12:00、15:00、18:00 各提醒一次喝水。这样设置吗？回复‘确认’即可。”

## 8. 技术方案

## 8.1 技术栈

- 后端语言：Python
- Web 框架：FastAPI
- Agent 框架：LangChain、LangGraph
- 模型：当前最新 DeepSeek 在线模型
- 生产基础设施：阿里云轻量应用服务器优先
- MVP 初期部署：阿里云轻量应用服务器，Docker Compose 部署全部服务
- 后续生产部署：阿里云 ECS、ACK 或容器服务
- MVP 初期数据库：PostgreSQL Docker 镜像同机部署
- 后续数据库：阿里云 RDS PostgreSQL
- MVP 初期缓存：Redis Docker 镜像同机部署
- 后续缓存：阿里云 Redis/Tair
- 消息队列：MVP 可使用 Redis 队列，后续升级 RabbitMQ、RocketMQ 或 MNS
- MVP 初期定时任务：APScheduler
- 后续定时任务：阿里云 EventBridge 或独立 Worker
- MVP 初期日志：本地结构化日志和日志轮转
- 后续日志与监控：阿里云 SLS、云监控、告警策略
- 对象存储：OSS
- 密钥管理：环境变量，后续升级阿里云 KMS
- 本地开发：Docker Compose

## 8.1.1 阿里云基础设施映射

MVP 初期优先使用阿里云轻量应用服务器，通过 Docker Compose 部署所有服务，降低固定成本和云产品复杂度。

| 模块 | 阿里云服务或 Docker 服务 | 用途 |
|---|---|---|
| 公众号回调服务 | 轻量应用服务器 + FastAPI Docker 镜像 | 接收微信公众号回调 |
| Agent 服务 | 轻量应用服务器 + App Docker 镜像 | 运行 LangChain、LangGraph 和业务逻辑 |
| 反向代理与 HTTPS | Nginx/Caddy Docker 镜像 | HTTPS、域名、转发到 FastAPI |
| 关系数据库 | PostgreSQL Docker 镜像，后续升级 RDS PostgreSQL | 保存用户、提醒、订阅、消息日志、长期记忆 |
| 缓存与短期状态 | Redis Docker 镜像，后续升级 Redis/Tair | 会话缓存、幂等控制、限流、短任务状态 |
| 异步任务 | Redis 队列，后续升级 RabbitMQ、RocketMQ 或 MNS | 执行资讯生成、消息推送、失败重试 |
| 定时调度 | APScheduler Worker Docker 镜像，后续升级 EventBridge | 触发每日简报、提醒扫描、access_token 刷新 |
| 文件存储 | 本地挂载卷，后续升级 OSS | 保存导出文件、调试附件、后续知识库文件 |
| 日志 | 本地结构化日志和日志轮转，后续升级 SLS | 应用日志、消息日志、错误日志 |
| 监控告警 | 轻量应用服务器基础监控，后续升级云监控和 SLS 告警 | 监控响应时间、失败率、推送异常、模型调用异常 |
| 密钥与权限 | 环境变量，后续升级 KMS/RAM | 管理微信密钥、DeepSeek API Key、数据库密码 |
| 后续 RAG | PostgreSQL pgvector 或向量检索服务 | 支持个人知识库、家庭知识库和文档问答 |

MVP 初期建议使用“阿里云轻量应用服务器 + Docker Compose + PostgreSQL Docker + Redis Docker + FastAPI Docker + APScheduler Worker Docker + Nginx/Caddy Docker”的低成本组合。正式内测或用户量上升后，再逐步升级为托管数据库、托管 Redis、SLS、EventBridge 或容器服务。

## 8.2 微信公众号服务

核心模块：

- 微信签名校验。
- XML 消息解析。
- 用户 OpenID 管理。
- access_token 管理。
- 客服消息或模板消息发送。
- 菜单配置。

需要注意：

- 微信被动回复有时间限制，复杂任务应异步处理。
- 主动推送能力取决于公众号类型和权限。
- 推送内容需要符合微信平台规范。

## 8.3 模型接入

使用当前可用的最新 DeepSeek 模型作为主模型，通过兼容 OpenAI SDK 或 LangChain ChatModel 适配器接入。

建议封装：

- `ChatDeepSeek`
- 模型配置管理。
- 超时和重试。
- token 使用统计。
- 降级策略。

已确认方案：

- 第一版使用最新 DeepSeek 在线模型。
- 当前不接入其他备用模型，但代码层预留备用模型配置位。
- 模型名称、上下文长度、价格和限流在开发前以 DeepSeek 官方控制台和文档为准。
- 业务代码不直接绑定具体模型 ID，通过统一 `LLMProvider` 或配置项管理。
- 基础设施部署在阿里云轻量应用服务器，模型层通过公网调用 DeepSeek API。
- DeepSeek 不属于阿里云基础设施，因此模型层需要独立的超时、重试、熔断和成本监控。

## 8.4 数据模型

详细数据库设计、字段含义、索引、状态枚举、Redis key 和验收清单见 `DATABASE_DESIGN.md`。PRD 仅保留核心表概览。

MVP 核心表：

- `users`：微信公众号用户与系统用户映射。
- `id_sequences`：用户对外编号生成。
- `user_profiles`：用户长期偏好和个性化记忆。
- `reminders`：提醒与待办。
- `subscriptions`：每日简报、天气、资讯等订阅。
- `message_logs`：用户消息、系统回复和 Agent 执行日志。
- `scheduled_jobs`：提醒、简报、token 刷新等定时任务。
- `wechat_tokens`：微信公众号 access_token 状态。
- `life_records`：生活记录预留表，MVP 可先建表后开放功能。

表关系以 `users` 为用户根表，提醒、订阅、偏好、消息日志和生活记录均通过 `user_id` 归属到具体用户；`scheduled_jobs` 关联提醒、订阅或系统任务；`wechat_tokens` 为系统级表。

## 8.5 定时任务

定时任务类型：

- 单次提醒触发。
- 周期提醒触发。
- 每日早报。
- 每日晚报。
- 订阅资讯更新。
- access_token 刷新。

建议：

- 使用数据库保存任务状态。
- 定时调度器只负责扫描和派发。
- 实际推送任务异步执行。
- 推送失败需要重试和日志记录。
- MVP 初期在轻量应用服务器的 APScheduler Worker 容器内扫描到期任务。
- 正式内测后可以升级为阿里云 EventBridge 或独立调度 Worker。
- 对可靠性要求更高后，将任务派发升级到 RabbitMQ、RocketMQ 或 MNS。

推荐策略：

- 提醒任务：使用数据库任务表记录，到期后由定时扫描器派发。
- 每日简报：使用订阅表和定时扫描器生成推送任务。
- access_token：使用独立定时任务刷新并写入 Redis。
- 失败重试：记录失败原因、重试次数和下一次重试时间。

## 8.6 部署方案

### 8.6.1 MVP 生产部署

MVP 初期推荐组合：

- 应用服务：阿里云轻量应用服务器。
- 部署方式：Docker Compose。
- 数据库：PostgreSQL Docker 镜像。
- 缓存：Redis Docker 镜像。
- 定时触发：APScheduler Worker Docker 镜像。
- 反向代理：Nginx/Caddy Docker 镜像。
- 日志：本地结构化日志和日志轮转。
- 密钥：环境变量。

正式内测升级组合：

- 应用服务：ECS、ACK 或容器服务。
- 数据库：阿里云 RDS PostgreSQL。
- 缓存：阿里云 Redis/Tair。
- 定时触发：EventBridge 或独立 Worker。
- 日志：SLS。
- 监控：云监控和 SLS 告警。
- 文件：OSS。
- 密钥：KMS。

### 8.6.2 后续扩展部署

当用户量和任务量上升后，可逐步升级：

- 异步任务从 Redis 队列升级到 RabbitMQ、RocketMQ 或 MNS。
- Agent 服务拆分为公众号网关、Agent Worker、推送 Worker。
- 数据库和 Redis 从轻量应用服务器同机容器迁移到阿里云托管服务。
- RAG 能力接入 PostgreSQL pgvector 或向量检索服务。
- 管理后台独立部署。
- 多环境拆分为开发、测试、生产。

## 8.7 安全与隐私

要求：

- 用户数据按 OpenID 隔离。
- 敏感配置通过环境变量管理，后续升级阿里云 KMS。
- 使用 RAM 控制云资源访问权限。
- 日志避免记录完整隐私内容。
- 用户可以查询和删除个人偏好。
- 对医疗、法律、金融问题添加免责声明。
- 管理后台需要鉴权。
- 数据库、Redis、内部服务在 Docker Compose 网络内访问；升级托管服务后应优先使用 VPC 私网访问。

## 9. 管理后台

MVP 可提供简单后台或命令行配置。

功能：

- 查看用户数量。
- 查看消息日志。
- 查看推送任务。
- 配置资讯源。
- 配置系统提示词。
- 配置模型参数。
- 手动触发测试推送。

可选方案：

- 初期使用 FastAPI Admin 或简单内部页面。
- 后续再做正式运营后台。

## 10. 用户体验设计

## 10.1 交互原则

- 用户用自然语言表达，系统负责理解。
- 复杂操作先确认。
- 回复短而清晰，适合微信阅读。
- 尽量减少用户输入成本。
- 对失败原因给出可操作建议。

## 10.2 回复风格

推荐风格：

- 温和、简洁、明确。
- 少用长篇解释。
- 重要信息使用列表。
- 时间、地点、金额等关键信息突出。

示例：

用户：“明天早上提醒我交水电费。”

系统：“好的，已为你设置提醒：明天 08:00 交水电费。”

## 10.3 异常体验

当模型无法理解时：

“我还没完全理解你的意思。你可以这样说：‘明早 8 点提醒我带身份证’，或‘每天早上 8 点给我发天气’。”

当工具失败时：

“这次没有设置成功，可能是服务暂时不可用。我已经记录失败日志，可以稍后再试。”

## 11. 成功指标

### 11.1 产品指标

- 新用户关注转化率。
- 首次有效对话率。
- 提醒创建成功率。
- 订阅创建成功率。
- 7 日留存率。
- 每用户每周主动使用次数。

### 11.2 技术指标

- 微信消息平均响应时间。
- Agent 意图识别准确率。
- 工具调用成功率。
- 定时推送成功率。
- 模型调用失败率。
- 平均单次对话成本。

## 12. MVP 范围

## 12.1 第一阶段必须完成

- 微信公众号接入。
- 文本消息收发。
- 最新 DeepSeek 模型调用。
- LangGraph 基础 Agent 工作流。
- 提醒创建、查询、删除。
- 每日资讯订阅和推送。
- 常见问题问答。
- 用户偏好设置。
- 公众号菜单与标签预留扩展入口。
- PostgreSQL Docker 基础数据模型。
- Redis Docker 短期状态与缓存。
- 本地结构化日志和日志轮转。

## 12.2 第一阶段暂不完成

- 语音输入。
- 图片识别。
- 复杂家庭协作。
- 完整运营后台。
- 多模型自动路由。
- 高级 RAG 知识库。
- 支付与商业化。

## 13. 里程碑

### 13.1 M0：需求确认

输出：

- PRD v1.0。
- 技术架构图。
- 数据模型草案。
- 微信公众号权限确认。
- 阿里云轻量应用服务器与 Docker Compose 资源清单。

### 13.2 M1：基础通路

输出：

- FastAPI 服务以 Docker 镜像部署到阿里云轻量应用服务器。
- 微信公众号回调接入。
- DeepSeek 模型调用。
- 基础 LangGraph 对话流程。
- PostgreSQL Docker、Redis Docker、本地日志接入。

### 13.3 M2：生活管理 MVP

输出：

- 提醒创建、查询、删除。
- APScheduler 定时任务触发。
- 微信推送。
- 消息日志。

### 13.4 M3：资讯与问答

输出：

- 天气与新闻接入。
- 简报生成。
- 订阅管理。
- 常见问题问答链路。

### 13.5 M4：内测优化

输出：

- 用户偏好。
- 错误兜底。
- 成本监控。
- 轻量应用服务器基础监控和日志排障。
- 小规模内测。

## 14. 风险与待确认问题

### 14.1 微信推送权限风险

主动推送能力强依赖公众号类型、认证状态和微信平台规则。

已确认：

- 使用已完成微信认证的服务号。
- 产品目标包含主动触达和用户互动即时回复。

仍需确认：

- 服务号后台具体可用的模板消息、订阅消息、客服消息权限。
- 不同消息类型的使用限制、频率限制和审核要求。

### 14.2 模型能力与成本风险

当前最新 DeepSeek 模型的实际能力、价格、上下文长度、稳定性、限流策略需要确认。

已确认：

- 使用当前最新 DeepSeek 模型。
- 当前无其他模型供应商，代码层预留备用模型扩展。

仍需确认：

- 实际模型 ID、上下文长度、价格、限流。
- 每日预算和单用户调用额度。

### 14.3 资讯合规风险

新闻摘要与推送可能涉及版权和来源合规。

已确认：

- 第一版使用天气 API、免费 RSS 或开放新闻接口。
- 内容展示为标题、短摘要和来源。
- 优先使用白名单来源。

仍需确认：

- 具体天气 API 和新闻 RSS/API 清单。
- 是否需要人工配置内容黑名单和关键词过滤。

### 14.4 隐私风险

生活管家会保存用户偏好、提醒、记录等个人信息。

已确认：

- 默认保存用户明确表达的长期偏好。
- 用户可以查询、修改、删除单条记忆，也可以删除全部长期记忆。

仍需确认：

- 隐私协议和用户授权文案。
- 数据保留周期。
- 是否保存健康、消费、证件等敏感生活记录。

### 14.5 云基础设施风险

MVP 初期基础设施优先依赖阿里云轻量应用服务器和 Docker Compose，可降低固定资源成本，但会带来单机部署、备份、监控和运维责任。

已确认：

- 后端基础设施优先使用阿里云轻量应用服务器。
- MVP 初期所有服务基于 Docker 镜像部署。
- 模型层调用外部 DeepSeek API，业务层通过模型供应商抽象解耦。

仍需确认：

- 首期阿里云地域选择。
- 轻量应用服务器配置、安全组、数据盘和备份策略。
- Docker Compose 同机 PostgreSQL、Redis、APScheduler 的稳定性边界。
- 后续升级到 RDS PostgreSQL、Redis/Tair、SLS、OSS、EventBridge、ACK/RocketMQ 的预算。
- DeepSeek API 出网访问稳定性和超时策略。

## 15. 后续讨论重点

已确认：

1. 使用已认证服务号，支持主动推送和互动回复。
2. MVP 聚焦“提醒/待办 + 每日简报 + 生活问答”。
3. 资讯采用天气 API 加免费 RSS 或开放新闻接口，保留可配置资讯源。
4. 使用当前最新 DeepSeek 模型，代码层预留备用模型。
5. 默认保存长期偏好，用户可删除记忆。
6. MVP 初期使用阿里云轻量应用服务器和 Docker Compose，模型层保持可插拔；后续再升级阿里云托管服务。

下一轮建议讨论：

1. 第一版是否需要管理后台，还是先用配置文件和数据库管理。
2. 提醒功能的具体边界：是否支持周期提醒、稍后提醒、贪睡提醒。
3. 每日简报默认内容和推送时间。
4. 用户隐私协议与数据删除策略。
5. 轻量应用服务器首期规格、地域、备份和升级路径。
